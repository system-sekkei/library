AWSTemplateFormatVersion: "2010-09-09"
Description: "library vpc"
Parameters:
  Flavor:
    Type: String
    Default: library
  VpcStack:
    Type: String
    Default: "library-vpc"

Resources:
  PublicALBSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupName: !Sub "${Flavor}-public-alb-security-group"
      GroupDescription: !Sub "${Flavor} public ALB SecurityGroup"
      VpcId: !ImportValue
        Fn::Sub: "${VpcStack}:VPC"
      SecurityGroupEgress:
        - CidrIp: 0.0.0.0/0
          Description: all egress allowed
          IpProtocol: -1
      SecurityGroupIngress:
        - CidrIp: 0.0.0.0/0
          Description: http ingress (provisional)
          IpProtocol: tcp
          FromPort: 80
          ToPort: 80
      Tags:
        - Key: Flavor
          Value: !Ref Flavor
        - Key: Name
          Value: !Sub "${Flavor}-public-alb-security-group"
  PublicALB:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: !Sub "${Flavor}-alb"
      Type: application
      Scheme: internet-facing
      IpAddressType: ipv4
      SecurityGroups:
        - !Ref PublicALBSecurityGroup
      Subnets:
        - !ImportValue
          Fn::Sub: "${VpcStack}:PublicSubnet1"
        - !ImportValue
          Fn::Sub: "${VpcStack}:PublicSubnet2"
      Tags:
        - Key: Flavor
          Value: !Ref Flavor
  PublicALBHttpListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref PublicALB
      Port: 80
      Protocol: HTTP
      DefaultActions:
        - Type: fixed-response
          FixedResponseConfig:
            ContentType: text/plain
            StatusCode: 418
            MessageBody: Would you like a cup of coffee?
Outputs:
  PublicALB:
    Value: !Ref PublicALB
    Export:
      Name: !Sub "${AWS::StackName}:PublicALB"
  PublicALBDNSName:
    Value: !GetAtt PublicALB.DNSName
